CI trigger

This file is intentionally small — commit and push to trigger the `test-backend` workflow.

Run locally:

```bash
git add CI_TRIGGER_COMMIT.md
git commit -m "ci: trigger test-backend"
git push origin main
```
